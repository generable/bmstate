# Plot cumulative hazard of 'msfit'

Plot cumulative hazard of 'msfit'

## Usage

``` r
msfit_plot_cumhaz(msfit, legend = NULL)
```

## Arguments

- msfit:

  An `msfit` object

- legend:

  transition name legend
